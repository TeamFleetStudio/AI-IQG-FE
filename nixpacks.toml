# Nixpacks configuration for Next.js deployment
# Nixpacks will auto-detect Next.js automatically

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"

